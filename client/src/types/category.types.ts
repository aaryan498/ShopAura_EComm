

export interface Category {
    id: string,
    name: string,
    description: string,
    imageUrl: string | null,
    slug: string,
    isActive: boolean,
    createdAt: string,
    updatedAt: string,
}